#1.Make sure you have the storage account to store the tfstate by using az cli (this will be performed for a single time).
./create_storage_account.sh <FILL_WITH_UNIQ_STG_NAME> westeurope Standard_LRS tfstate bootcamp-terraform westeurope

#2.Terraform steps to run to provision the infrastructure.
chmod +x deploy.sh; ./deploy.sh

#3.Terraform step to run to destroy the infrastructure.
chmod +x destroy.sh; ./destroy.sh

Note: Terraform v0.12.21
